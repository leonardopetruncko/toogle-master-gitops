apiVersion: v1
kind: Service
metadata:
  name: targeting-service
  namespace: toogle-master
spec:
  selector:
    app: targeting-service
  ports:
    - protocol: TCP
      port: 8003
      targetPort: 8003
  type: ClusterIP
